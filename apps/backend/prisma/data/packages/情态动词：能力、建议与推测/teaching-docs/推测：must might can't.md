# 用证据表达把握程度

情态动词可以组成一条“确信度刻度”。说话者根据证据选择不同表达，而不是陈述已确认的事实。

| 判断 | 情态动词 | 例子 |
| --- | --- | --- |
| 高度确信是真的 | `must` | `She must be busy.` |
| 有可能 | `may` / `might` / `could` | `She might be busy.` |
| 高度确信不是真的 | `can't` | `She can't be at work.` |

推测中的 `must` 是“一定是”，不是“必须做”。`He must be tired.` 是判断他的状态；`He must rest.` 才是要求他休息。

> The lights are off and the door is locked. They might be asleep, but they can't be in the office. They must be at home.

## 证据任务

看到一把湿雨伞、一只空咖啡杯和一台开着的电脑。分别用 `must`、`might`、`can't` 作出推测，并为每句话补充证据。
