# 离线学习、学习包与沉浸式播放器手册

> **用途**：漫语町离线学习包、今日任务、数据同步与沉浸式播放器的唯一权威参考。
> **说明**：本文档合并自《学习包离线与今日任务系统》《学习库沉浸式播放器设计》《离线同步学习包代码审查与优化方案》《离线模块代码异味与边界问题深度扫描》《离线模块重复代码与组件化重构方案》，内容以当前代码实现为准（核对日期：2026-08-11）。
> **适用代码**：
> - 前端离线：`apps/frontend/src/lib/offline/`（learning-pack.service、offline-sync.service、sync-outbox、asset-cache.service、daily-practice.repository、utils、logger 等）
> - 前端沉浸式播放器：`apps/frontend/src/features/learning/components/immersive-player/`
> - 后端同步：`apps/backend/src/modules/sync/`
> - 学习包后台：`apps/backend/src/modules/admin/`（learning-pack-admin）

---

## 目录

1. [架构总览](#1-架构总览)
2. [学习包架构（zip 模式）](#2-学习包架构zip-模式)
3. [离线存储与数据结构](#3-离线存储与数据结构)
4. [学习计划与今日任务](#4-学习计划与今日任务)
5. [数据同步（outbox + pull）](#5-数据同步outbox--pull)
6. [沉浸式播放器](#6-沉浸式播放器)
7. [代码质量现状与问题追踪](#7-代码质量现状与问题追踪)
8. [边界处理与风险](#8-边界处理与风险)
9. [后续规划](#9-后续规划)
10. [相关文档](#10-相关文档)

---

## 1. 架构总览

离线能力由三个独立但协作的子系统组成：

| 子系统 | 用途 | 存储介质 | 关键模块 |
|--------|------|----------|----------|
| **LearningPack**（学习包） | 场景、词汇、句块、知识点练习、离线 manifest | Capacitor SQLite | `learning-pack.service.ts` |
| **AssetCache**（资源缓存） | 图片、角色资源、知识点练习音频等二进制资源 | Capacitor Filesystem | `asset-cache.service.ts` |
| **MobileBundle**（OTA 热更新） | Web App zip 增量更新 | COS → Capacitor 原生插件 | `mobile-updates.service.ts` |

> **重要区分**：MobileBundle 是 **App 更新**（代码级），LearningPack 是 **内容下载**（数据级）。

### 设计原则

- **数据与资源分离**：结构化数据（JSON）→ SQLite；二进制资源（mp3/png）→ Filesystem。避免 SQLite 大 blob 的读写放大与 WKWebView 加载问题。
- **SHA256 去重 + 完整性校验**：同一资源跨包共享只存一份，下载后校验防传输损坏。
- **iOS WKWebView 兼容**：本地资源必须 `Capacitor.convertFileSrc()` 转 `capacitor://localhost` scheme。
- **引用计数卸载**：卸载包时检查其他包引用，防误删共享文件。
- **离线优先 + 出队同步**：本地先写 → outbox 入队 → 联网 flush。
- **状态机**：下载 `missing → downloading → ready` / `failed → 可重试`；任务 `queued → downloading/extracting → done/error`，退后台 `paused`。

---

## 2. 学习包架构（zip 模式）

> 状态：✅ 已落地为当前 zip 模式；delta 增量更新仍作为扩展能力保留。

### 2.1 数据模型（LearningPackage）

```prisma
model LearningPackage {
  id            String        @id @default(cuid())
  sceneId       String        // 固定：1 Scene = 1 Pack
  version       String        // 语义化版本 "1.0.0"
  title         String
  description   String?       @db.Text
  coverUrl      String?
  fileAssetId   String?       // zip 上传 COS 后的 FileAsset
  checksum      String?       // 全量 zip SHA256
  fileSize      Int?
  manifestSnapshot Json?      // manifest 快照（存 DB 用于增量 diff）
  status        PackageStatus @default(draft)  // draft | published | archived
  isMandatory   Boolean       @default(false)
  releaseNotes  String?       @db.Text
  minAppVersion String?
  downloadCount Int           @default(0)
  installCount  Int           @default(0)
  publishedAt   DateTime?
  @@unique([sceneId, version])
  @@map("learning_package")
}
```

### 2.2 zip 结构

```
learning-pack-{packId}-v{version}.zip
├── manifest.json          # 包元数据
├── checksums.json         # 逐文件 SHA256 清单
├── content/
│   ├── scene.json
│   ├── topics/{topicId}.json
│   ├── episodes/{episodeId}.json
│   ├── inks/{inkScriptKey}.json
│   └── indexes/           # 可选：派生索引
└── assets/
    ├── backgrounds/
    ├── characters/{charId}/
    └── audio/warmup/
```

> zip 是**传输格式**。客户端解压后：资产文件按 SHA256 存入全局池（Filesystem），内容 JSON 写入 SQLite。

### 2.3 后台管理 API

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` / `POST` | `/admin/learning-packs` | 列表 / 创建 |
| `POST` | `/admin/learning-packs/:id/generate` | 触发打包（异步生成 zip + manifest + 上传 COS） |
| `POST` | `/admin/learning-packs/:id/publish` | 发布 |
| `POST` | `/admin/learning-packs/:id/archive` | 归档 |

### 2.4 客户端安装流程

```
① 触发版本检查（App 启动/resume 延迟执行）
② POST /learning/packs/check → 上报已安装列表 → 写入 availablePackUpdates
③ 用户触发下载（学习计划页任务 drawer）
④ GET /learning/units/:id/download-pack → 302 COS 签名 URL → 下载 zip
⑤ SHA256 校验 → @zip.js/zip.js 解压
⑥ 资产文件 → SHA256 校验 → 全局池（复用/写入）+ asset_refs 引用
⑦ 内容 JSON → SQLite（业务表 + 派生索引表）
⑧ 更新 downloaded_packs + 清理临时文件
```

阶段进度（非字节级）：0-15 下载 zip → 15-40 解析 manifest/写索引 → 40-75 读取校验资源 → 75-95 写入本地 → 99 收尾 → 100 完成；进度只允许前进。

### 2.5 跨包去重

- **资产文件**：`offline-assets/{sha256}.{ext}` 全局池 + `asset_refs`（sha256 + packId + logicalPath）引用计数；卸载时 refCount 归零才删物理文件。
- **内容数据**：`offline_vocabularies` / `offline_chunks` / `offline_patterns` 扁平记录 + `offline_content_refs` 引用关系。
- 存储收益参考：3 个共享资源的场景包可节省约 53%，典型混合场景约 30%。

### 2.6 环境变量

```bash
LEARNING_PACK_TMP_DIR=/tmp/learning-packs
LEARNING_PACK_DOWNLOAD_URL_EXPIRES=3600
LEARNING_PACK_BUILD_TIMEOUT_MS=300000
```

---

## 3. 离线存储与数据结构

### 3.1 Manifest 与 AssetRef

```typescript
interface LearningPackManifest {
  packId: string; version: number; title: string; updatedAt: string
  contentHash?: string
  units: string[]; topics: string[]; vocabularies: string[]
  chunks: string[]; sentencePatterns: string[]; scriptEpisodes: string[]
  inkScripts: string[]; assets: AssetRef[]
}

interface AssetRef {
  assetId?: string
  fileAssetId?: string   // 稳定文件资产 ID（跨签名刷新）
  alias?: string         // Ink 标签别名（如 bg_city）
  url: string
  path?: string
  sha256?: string
  mimeType?: string
  size?: number
  role?: 'background' | 'sprite' | 'voice' | 'bgm' | 'sfx' | 'thumbnail' | 'warmup_audio' | 'warmup_image' | 'immersive_audio'
}
```

### 3.2 SQLite 表结构（DB_VERSION = 10）

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `downloaded_packs` | 已安装的学习包 | packId, manifest, status, installedAt |
| `downloaded_unit_details` | 场景/话题详细数据 | unitId, topicId, detail (JSON) |
| `ink_scripts` | Ink 叙事脚本缓存 | unitId, topicId, inkJson |
| `local_assets` | 资源文件缓存状态 | remoteUrl, localUri, status, sha256 |
| `offline_vocabularies` / `offline_chunks` / `offline_patterns` | 离线内容 | id, word/text/pattern, data |
| `offline_content_refs` | 内容引用关系 | kind, contentId, packId |
| `asset_refs` | 资源引用计数 | sha256, packId, logicalPath |
| `warmup_records` | 整组作答明细（完整 JSON） | topicId, items, syncStatus |
| `warmup_record_entries` | warmup 题目级索引 | recordId, stepId, practicedDate, recordUpdatedAt |
| `daily_practice_items` | 知识点练习排期与掌握度 | itemId, packId, topicId, status, dueDate |
| `daily_practice_attempts` | 练习 attempt | itemId, score, practicedAt, syncStatus |
| `daily_practice_runs` | 当日任务集合与完成状态 | date, scope, scheduledItemIds, completedItemIds |
| `outbox` | 离线操作出队 | entityType, operation, payload, status, syncedAt |

warmup 双表设计：`warmup_records` 保存一次 session 完整 JSON（同步 payload 需要），`warmup_record_entries` 按题目拆开（今日记录、最近答案、覆盖更新的高频查询走索引）。

### 3.3 本地文件路径

```
Directory.Data/offline-assets/
    ├── {sha256}.mp3 / .png / .webp ...
```

### 3.4 练习测试数据清理

存储管理 drawer「清空练习测试数据」清理：`user_progress`、`practice_records`、`warmup_records`、`warmup_record_entries`、`daily_activity`、`daily_progress`、`daily_practice_items/runs/attempts`，清除 practice 相关 outbox 与 guided warmup localStorage key，并写入本地 reset marker（隐藏清理时间之前 pull 回来的旧记录，非后端删除）。

---

## 4. 学习计划与今日任务

### 4.1 核心概念

```text
学习计划 = 用户选择的内容池（UserSceneProgress，最多 3 个未完成单元）
今日任务 = 从当前内容池里按天抽取的输出训练（TodayTaskPage 直接从离线学习包生成）
```

- 今日任务**不调用**服务端 `/learning/today`（旧接口保留但非主链路），直接从已安装学习包的 `trainingTopics[].metadata.outputTraining.pipeline[]` 生成。
- `writing/reading/listening/novel/story` 包不进入今日任务；Today 候选按 `contentMode=practice` 白名单筛选（后端完成接口重复校验）。

### 4.2 后端链路

- `GET /learning/my-units` — 我的学习（UserSceneProgress 关联 Scene/分类/Topic，词汇/句块按 id 去重计数）
- `POST /learning/units/:id/start` — 开始学习（权限校验 + 最多 3 个未完成 + upsert）
- `GET /learning/units/:id`、`/learning/units/:id/download-pack`、`/learning/units/:id/download-delta`
- `POST /learning/packs/check` — 版本检查

### 4.3 前端链路

1. `LearningPlanPage` → `learningRepository.getMyUnits()`（本地优先，无则请求远端）
2. `MyLearningView` 按 `downloaded_packs` 判断安装状态；未安装阻止进入详情
3. 加入学习：本地写 `my_learning_units` + outbox 入队 → 在线调 `/start` → 刷新 → 触发下载任务
4. `TodayTaskPage`：URL packId 优先 → 本地已安装包 → 我的学习第一个已安装包 → 空态引导

### 4.4 今日任务生成

- 抽题来源：`pipeline[]`；支持 `chunk_substitution` / `vocab_drill` / `vocab_sentence_building` / `pattern_drill` / `sentence_decomposition`
- 音频传递：题目保留 `audioUrl` + `audioAssetId`；句子拆解音频在 `levels[]`、替换/句型在 `items[]`、一词多句在 `patterns[].items[]`
- 题量：`dailyGoal`（preferences），先按题型多样性（每种取 1）再随机补足
- 排期：与学习本统一的 `Again/Hard/Good/Easy` SM-2 状态机；客户端先行计算，同步时后端按 `clientAttemptId` 幂等保存 attempt 并重放排期返回权威进度

### 4.5 音频策略（useCachedAudio）

1. Capacitor 优先本地 zip 解出的 mp3
2. 本地记录存在但文件丢失 → 删坏记录 → `audioAssetId` 换 private URL 重新下载缓存
3. Web 只要有 `audioAssetId` 播放前都请求 `/file-assets/:id/private-url`
4. 播放失败再用 fresh private URL 兜底

> 内容作者应优先保存并传递 `audioAssetId`；`audioUrl` 只作旧数据兼容。

---

## 5. 数据同步（outbox + pull）

### 5.1 写入路径（离线优先）

```
用户操作 → 本地 SQLite 即时反馈 → outbox 入队 → 在线时 flush 推送
```

- `warmup_records`（含 entries）→ `/sync/push` → 后端 `PracticeWarmupRecord`
- 今日任务 run/attempt → `/practice/daily-practice/complete`（失败时入队 `daily_practice` outbox 重试）
- 表达库（word/chunk/pattern entry）→ 本地写 + outbox → 在线立即 HTTP + `markSynced`
- 同步成功只改变 `syncStatus` / `remoteId`，不改变题目"最近作答时间"

### 5.2 push（flush）

- 按 outbox 批次处理：`replayItem` 按 `entityType` 分发（my_unit / practice_session / practice_turn / warmup_records / daily_practice / word_entry / chunk_entry / pattern_entry / learning_pack / expression…）
- 依赖未准备好 → `markFailed`；永久失败 → `markDiscarded`（标记 synced + discarded 原因）；成功 → `markSynced`
- 表达类操作预取远端全量列表复用（避免逐条 list）

### 5.3 pull（增量拉取）

- 后端 `/sync/pull`：**按类型独立 cursor**（`maxTime` 取该类型返回记录的最大时间戳），每类每页 500 条（`PULL_PAGE_SIZE`），返回 `cursors + hasMore + changed/deleted`
- 前端 `pull()` 用 `while(true)` 循环直到所有类型 `hasMore=false`，累计应用 `applyUserPullChanges`（expressionItems / sceneProgresses / chunkProgresses / practiceSessions / practiceWarmupRecords / topicSessions / deletedExpressionItems）
- 今日任务 item 权威进度拉取后收敛本地，**不覆盖有未同步 attempt 的项**

### 5.4 outbox 记录生命周期

- `pending → synced`（标记而非删除，**保留 7 天审计追溯**，`cleanup()` 统一清理）
- `pending → failed`（保留，下次 flush 重试）
- 永久失败 → `markDiscarded`（标记 synced + `lastError: discarded`）

---

## 6. 沉浸式播放器

> 入口：我的学习库（`ExpressionLibraryPage`）与学习包知识点，通过 `mapInsightItemsToImmersiveItems` 映射队列打开 `ImmersivePlayerDialog`。

### 6.1 信息架构

- 不使用 tab；主视图 = 核心文本（单词/句块/句型）+ 中文意思/见解 + 一个代表例句（可选译文）
- 展示优先级：word → `meaning` 其次 `definitionEn`；chunk → `meaning + description`；pattern → `example` 其次 `examples[0].en`

### 6.2 播放队列模型

```typescript
type ImmersivePlayerItemKind = 'word' | 'chunk' | 'pattern'
type ImmersivePlayerItem = { id, kind, title, meaning?, insight?, exampleEn?, exampleZh?, audio?: { mainTextUrl?, exampleUrl? }, sourceSnapshot? }

type PlaybackSegment = {
  itemId: string
  role: 'main' | 'meaning' | 'example' | 'exampleTranslation'
  text: string
  audioUrl?: string | null
  lang: 'en' | 'zh'
}
```

每个 item 按设置展开成片段组（main → meaning → example → exampleTranslation）；`repeatPerItem` 重复整个片段组。

### 6.3 设置与持久化

`ImmersivePlaybackSettings`（persist 到 preferences）：sleepTimer（0/15/30/60）、repeatPerItem（1/2/3/infinite）、playbackRate（0.75/1/1.25/1.5）、playMainText、playMeaning、playExample、playExampleTranslation、autoNext、loopQueue、textVisible。

### 6.4 音频来源与后台播放

- **Web 前台**：`useCachedAudio` + 按需 TTS（`synthesizeAsset`，bizType=`immersive_player`）；片段按需合成，失败跳过不阻断队列。
- **Native**：`@capgo/capacitor-native-audio` + `@capgo/capacitor-media-session` 桥接；锁屏元数据规则 `title=当前词句`、`artist=释义/例句`、`album=学习库沉浸式播放`；片段由 JS 收到原生 `complete` 事件后启动下一段。
- **已知边界**：iOS 需真机验证 Background Modes（Audio）；Android 原生工程不在仓库，前台服务/媒体通知未验收。若真机出现"当前片段播完就停"，需把整条队列下沉到原生 AVQueuePlayer/Media3，**禁止**静音保活或定时器绕过。

### 6.5 分阶段状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 前台播放器 MVP（统一队列、设置 Drawer、隐藏文案、播放控制） | ✅ 已实现 |
| Phase 2 | 音频资产预处理（按需 TTS、本地缓存优先、FileAsset 私有地址） | ✅ 基本实现（长队列预取可继续优化） |
| Phase 3 | 原生后台播放与锁屏控制 | 🟡 桥接已实现，原生交付配置待真机验收 |
| Phase 4 | 混合队列、只听收藏/未掌握、随机、迷你播放器、后台学习记录 | ⬜ 未开始 |

---

## 7. 代码质量现状与问题追踪

> 以下合并自三份审查文档，标注**代码核对（2026-08-11）后的最新状态**。

### 7.1 已修复 ✅

| 问题 | 原风险 | 修复方式 |
|------|--------|----------|
| `syncOutbox.markSynced` 直接删除 | 无法审计 | 改为标记 `synced` + `syncedAt`，保留 7 天由 `cleanup()` 清理；新增 `markDiscarded` 对称处理永久失败 |
| 后端 pull 游标设计缺陷（共享时间戳数组） | 跨类型漏同步 | 重写为按类型独立 cursor（`maxTime`）+ 每页 500 + `hasMore` 分页 |
| `MAX_PULL_PAGES = 5` 硬限制 | cursor 更新后剩余数据丢失 | 已移除；前端 `while(true)` 循环直到 `hasMore=false` |
| daily_practice 不走 outbox | 离线完成的今日任务不同步 | `completeRun` / `syncAdHocRun` 失败时入队 `daily_practice` outbox 重试 |
| `_processDownloadQueue` 死代码 | 混淆维护者 | 已删除 |
| 日志系统不一致（各文件自定前缀） | 调试效率低 | `lib/offline/logger.ts`：统一 `createLogger(scope)` + 级别控制（dev=info，prod=warn） |
| 工具函数重复（toIsoString/createId/errorMessage/resolveSessionId） | 多份副本易漂移 | `lib/offline/utils.ts` 统一导出；`resolveSessionId` 明确返回 null 语义（未同步不得传本地 id 给远端 API） |
| asset-cache SHA-256 实现 | 死代码/不可用 | 重写为纯 JS 完整实现（`sha256Pure`/`digestSync`，兼容无 crypto.subtle 环境）；新增 `saveFromBufferWithSha256` 复用 zip 安装期预计算哈希 |
| 沉浸式播放器（设计稿） | — | Phase 1/2 已落地（见第 6 章） |

### 7.2 仍未修复 ❌（按优先级）

| 优先级 | 问题 | 位置 | 说明 |
|--------|------|------|------|
| P1 | `installUnitFromZip` 方法过长（~450 行） | `learning-pack.service.ts`（现 1053 行） | 建议拆 zip-reader / content-persister / asset-extractor / install-orchestrator |
| P1 | 安装失败无回滚 | learning-pack 安装流程 | 残留半成品数据影响重试；建议启动时清理 `status='installing'` 且超时的包 |
| P1 | `replayItem` 巨型 if-else 链（~200 行） | `offline-sync.service.ts`（现 783 行） | 建议拆注册制 handler（`sync-handlers/`） |
| P1 | `applyUserPullChanges` 机械映射 | offline-sync.service.ts | 建议声明式 pull mapping 配置 |
| P2 | `asset_refs` 全表扫描 | learning-pack.service.ts | `refsBySha256` Map 预加载（key 非唯一键）；多包时性能下降，建议索引查询 |
| P2 | 32 处静默 `catch {}` | 各离线文件 | 建议 `safeCall()` 包装 + 至少 console.debug；仅限原本空 catch 的位置 |
| P2 | 大量 `any` 类型（~150 处） | 各离线文件 | 补充核心接口类型 |
| P2 | 下载完成 3 秒延迟清除 | 下载任务状态 | 反模式，改为状态驱动清理 |
| P3 | `CachedDataSource<T>` 泛型类（本地优先→远端兜底） | 各 repository | 15 个 `get*` 方法重复 25-40 行/处 |
| P3 | `usePackDownloadQueue` hook 抽离 | `learning.store.ts` | 下载任务持久化逻辑可抽 hook |
| P3 | 磁盘空间预检（zip 3 倍） | 下载入口 | 无预检查 |
| P3 | 登录态过期暂停 outbox 同步 | flush | 避免 401 无效重试风暴 |
| P3 | SQLite 初始化失败降级 | `initBackend` | 上层无统一降级 |

### 7.3 建议重构目标结构（参考）

```
lib/offline/
├── index.ts / utils.ts / logger.ts / error-utils.ts / cached-data-source.ts
├── sync/（sync-api / sync-outbox / sync-engine / sync-pull-mappings / handlers/registry + 各 handler）
├── pack-installer/（zip-reader / content-persister / asset-extractor / install-orchestrator）
├── repositories/（learning / practice / learning-content / learning-notebook / daily-practice）
├── asset-cache.service.ts / offline-storage.service.ts / warmup-record-index.ts / unified-storage.ts
└── sqlite/（schema / sqlite-json-store / sqlite-storage / web-sqlite-storage）
```

---

## 8. 边界处理与风险

| 边界场景 | 当前处理 | 状态 |
|----------|----------|------|
| App 被杀死（安装中） | 恢复 paused 状态，SQLite 可能残留半成品 | ⚠️ 无回滚清理（见 7.2 P1） |
| App 被杀死（同步中） | outbox 保留 pending，下次重试 | ✅ 合理 |
| 网络中断（安装中） | AbortError 捕获，任务标记 error | ⚠️ 残留数据不清理 |
| 网络中断（同步中） | 单项失败标记 failed 重试 | ✅ 合理 |
| 多个包同时安装 | `MAX_CONCURRENT_DOWNLOADS = 2` | ✅ 合理 |
| 同一包重复安装 | 替换旧任务 | ⚠️ 旧任务 AbortController 未清理 |
| 登录态过期 | Axios 拦截器跳登录 | ⚠️ outbox 无效重试（见 7.2 P3） |
| SQLite 不可用 | initBackend 抛异常 | ⚠️ 无统一降级 |
| 锁屏后台播放 | Native 桥接已实现 | ⚠️ iOS/Android 原生配置待真机验收 |

---

## 9. 后续规划

- **Delta 增量包**：`DeltaPackage` 表 + 发布时自动生成 delta zip + 增量下载合并降级
- **安装统计**：downloadCount / installCount 落地报表
- **沉浸式播放器 Phase 3**：iOS Background Audio capability 真机核验；Android 原生工程纳入 + Foreground Service / MediaSession
- **沉浸式播放器 Phase 4**：混合队列、只听收藏/未掌握、随机播放、迷你播放器、后台学习记录
- **重构 Phase B/C**：CachedDataSource、sync handler 注册制、AssetExtractor 抽离（见 7.2/7.3）

---

## 10. 相关文档

- [产品功能现状与实施路线图](./产品功能现状与实施路线图.md)
- [剧情内容与 Ink 剧本指南](./剧情内容与Ink剧本指南.md)
- [输出练习链路设计方案](./输出练习链路设计方案.md)
- [会员额度与生产运营手册](./会员额度与生产运营手册.md)
